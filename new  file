#include <stdio.h> 

int main() {
	// Read the number of test cases.
	int T;
	scanf("%d", &T);
	while (T--) {
		// Read the input a, b
		int n,a, b;
		scanf("%d %d %d", &n,&a, &b);

		// Compute the ans.
		int ans = a + b;
		printf("%d\n", 2*(180+n)- ans);
	}

	return 0;
}
